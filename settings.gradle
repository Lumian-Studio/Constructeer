final def platforms = [
	'fabric',
	'neoforge'
]

// Our conventions
includeBuild('build-logic')

// For each platform, we build an extra project hierarchy
platforms.forEach { plat -> includeBuild("platform-${plat}") }
