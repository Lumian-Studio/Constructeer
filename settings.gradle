pluginManagement {
	repositories {
		maven {
			name = 'Fabric'
			url = 'https://maven.fabricmc.net/'
		}
		mavenCentral()
		gradlePluginPortal()
	}
}

def modules = [
	'core',
	'multimining'
]

rootProject.name = "Constructeer"
includeBuild('build-logic')
modules.each { include("cteer-${it}")}
